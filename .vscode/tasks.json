{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clang build",
            "type": "shell",
            "command": "clang",
            "args": [
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "main.c"
            ],
            "group": "build"
        },
        {
            "label": "run",
            "type": "shell",
            "command": "${fileDirname}/${fileBasenameNoExtension}",
            "group": "build",
            "problemMatcher": [],
        },
    ]
}